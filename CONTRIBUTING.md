# Contributing

Keep this repo Windows-native and lightweight.

- Do not add WSL, Docker, Linux rootfs, or bundled distro assets.
- Keep the local proxy allowlist narrow.
- Keep generated release artifacts out of Git.
- Run `npm run build` and the smoke test before release changes.
